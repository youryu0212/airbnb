export interface DateType {
  year: number;
  month: number;
  day: number;
}

export interface EventType {
  target: any;
}

export interface FlexType {
  flex: boolean;
  justify?: string;
  align?: string;
  direction?: string;
  wrap?: string;
}
